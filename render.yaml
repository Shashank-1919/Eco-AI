services:
  - type: web
    name: eco-ai
    env: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: MONGODB_URI
        value: mongodb+srv://shashankpandit1919:shashank%401919@cluster0.zisnna9.mongodb.net/ecoai?retryWrites=true&w=majority&appName=Cluster0
      - key: JWT_SECRET
        generateValue: true # Render will generate a strong secret
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
